{
  "name": "group-webhook-dispatch-skill",
  "private": true,
  "version": "0.1.0",
  "description": "Event-driven webhook dispatch for group tasks with run-once reconcile.",
  "scripts": {
    "dispatch": "tsx scripts/dispatch_webhook.ts",
    "reconcile": "tsx scripts/reconcile_webhook.ts",
    "upsert-plan": "tsx scripts/upsert_webhook_plan.ts"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "tsx": "^4.20.3",
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "commander": "^13.1.0"
  }
}
